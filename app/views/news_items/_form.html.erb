<%= form_for(@news_item) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <div class="field">
        <%= f.text_area :content, placeholder: "Напишите новость..." %>
    </div>
    <%= f.submit yield(:button_text),  class: "btn" %>
<% end %>
